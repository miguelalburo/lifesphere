# To-Do List

## Recap

- Analysis-results layer implemented (differential expression, differential
  methylation, GWAS association).
- New Stage-2 reshaper `src/standardise/analysis_reshape.py`, wired into
  `src.standardise.run` (runs before file discovery; no-op when no manifest present).
- Reification pattern extended: results anchor to a `Contrast` (cohort comparison)
  instead of a `Sample`, reusing the shared feature dimensions
  (`Gene`/`CpGSite`/`Variant`) and adding a `Trait` dimension for GWAS.
  `(Analysis)-[:PRODUCED]->(Contrast)-[:HAS_*]->(<Result>)-[:FOR_*]->(<Feature>)`.
- Schema synced across all three sync points (`entities.json`, `edges.json`,
  `schema_config.yaml`). Verified: 3-modality smoke run, `validate --strict` clean
  (0 dangling), 57/57 pytest.
- Input formats documented in `docs/dev_guides/analysis_results.md`.


## Items to Complete

1. Test coverage
    - Add `tests/fixtures/analysis_smoke/` (mirror `tests/fixtures/omics_smoke/`)
      with a manifest + one table per modality.
    - Add a pytest exercising the reshaper + generic engine so the path is in CI.

3. Provenance linkage
    - Link `Analysis` / result tables to a `ProcessedDataOutput` node (source-file
      checksum/path), matching the omics `FROM_OUTPUT` pattern, so the raw summary-stat
      file is retained even when GWAS rows are thresholded out at import.


5. Real-data trial
    - Run against an actual DESeq2 / limma / REGENIE output set and confirm column
      mappings + thresholds behave as expected end-to-end into Neo4j.
