## Recap

Just completed enforcing v2 repo wide, with some fresh designs across the code logic. BlueBEAR job failed on omics (files too large)

### Items-to-complete

- Some clinical nodes (such as tissue, organ, organism) are not properly extracted. Investigate then plot.

- Omics end-to-end. Build profiles and standardisation. Modify extraction to obtain omics or clinical seperately.
